<div class="button">
    <button type="button" data-toggle="collapse"
    data-target="#{{$id}}" class="moreDetailsButton {{!isset($expanded) ? 'collapsed' : ''}} {{isset($classes) ? $classes : '' }}">
    </button>
</div>